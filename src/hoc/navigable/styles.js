import { StyleSheet } from 'react-native';

import { black } from '../../lib/colors';

export default StyleSheet.create({
  header: {
    height: 100,
  },
  title: {
    fontSize: 16,
    color: black,
  },
});
